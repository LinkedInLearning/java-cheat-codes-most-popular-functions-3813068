package com.example.tochararraymethod;

public class Answer {

    // Stores the original string
    private String originalString;

    public Answer(String originalString) {
        this.originalString = originalString;
    }

    // **Challenge:** Implement a method named `toCharArray()` that returns a new
    // character array containing the characters from the original string.
    // Utilize the built-in `toCharArray()` method of the String class and
    // return the resulting character array.

    /*
     * // This commented section is the solution, remove the comments
     * // and complete the method yourself to understand the concept.
     * public char[] toCharArray() {
     * return originalString.toCharArray();
     * }
     */
}
